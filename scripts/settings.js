export const ORIGIN_FOLDER = 'data';
export const     MODULE_NAME = 'ensemble';
// the path of the folder where all the uploaded images are saved
export const UPLOAD_FOLDER_PATH = `uploaded-${MODULE_NAME}`;
// determines if we should display debug
export const DEBUGGING = false;
// Determines if the debug should have a trace history
export const TRACE = false;
export const PLAYLIST_NAME = "Dropped Sounds";
export const CONSOLE_MESSAGE_PRESET = [`%c${MODULE_NAME} %c|`, 'background: #222; color: #bada55', 'color: #fff'];